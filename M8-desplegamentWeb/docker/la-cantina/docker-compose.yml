version: '3.8'

services:
  apache:
    image: httpd:2.4
    container_name: cantina_apache
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/local/apache2/htdocs
      - ./conf:/usr/local/apache2/conf/extra
      - ./logs:/usr/local/apache2/logs
    restart: unless-stopped
